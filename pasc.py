import os
import time
import math

for t in 'Tamizh arasan':
	print'Name:',t[:4]
	time.sleep(0.1)

n = int(input('Enter the number:'))
fru = ['1','2','3','4','5']
for x in fru:
		x+= n
		print(x)
		print("\n")
		
			